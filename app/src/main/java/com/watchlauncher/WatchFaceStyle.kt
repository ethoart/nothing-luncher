package com.watchlauncher

enum class WatchFaceStyle(val displayName: String) {
    NOTHING_DOT("Nothing OS"),       // White bg, dot-matrix font, red accent
    BOLD_DIGITAL("Bold Digital"),    // Black bg, huge white time, minimal
    NEON_MINIMAL("Neon"),            // Black bg, cyan/green accent
    RETRO_ORANGE("Retro"),           // Black bg, orange bold font
    CLEAN_WHITE("Clean White")       // Pure white, black text, no frills
}
